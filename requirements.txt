# PollenCounter-ObjectDetection-YOLO
# Python 3.10+ | YOLOv11 (yolo11n.pt)
#

#
# Core dependencies — torch/torchvision pulled transitively by ultralytics.
ultralytics>=8.4
opencv-python>=4.12.0
pandas>=2.2.0
openpyxl>=3.1.0
sahi>=0.11.14

# Install PyTorch with CUDA 12.4 support if needed:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124 --force-reinstall
